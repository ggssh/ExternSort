#ifndef TOOLS_H
#define TOOLS_H

#include <iostream>
#include <fstream>
#include "Windows.h"
#include <time.h>
using namespace std;
char *temp_filename(int index);
void init_data(int n);
void m_swap(int &a,int &b);
#endif // TOOLS_H
